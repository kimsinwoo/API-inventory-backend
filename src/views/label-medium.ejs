<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <style>
    @page { size: 80mm 60mm; margin: 0; }
    * { box-sizing: border-box; }
    html, body { margin:0; padding:0; font-family: 'Nanum Gothic', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif; -webkit-print-color-adjust: exact; print-color-adjust: exact; }
    .label { width: 80mm; height: 60mm; padding: 2.5mm; border: 0.2mm solid #ccc; display:flex; flex-direction:column; }
    .title { font-size: 14pt; font-weight: 700; color: #111; margin-bottom: 2mm; }
    .grid { font-size: 7pt; line-height:1.4; color:#111; margin-bottom: 2mm; }
    .foot { margin-top:auto; display:flex; justify-content:space-between; align-items:flex-end; gap: 2mm; }
    .barcode img { max-width:100%; height:auto; }
    .meta { font-size: 8pt; text-align:right; }
    .mw { text-align:center; }
    .mw small { display:block; font-size:7pt; font-weight:700; margin-top:1mm; }
  </style>
</head>
<body>
  <div class="label">
    <div class="title"><%= productName || '제품명' %></div>
    <div class="grid">
      <% if (registrationNumber) { %>
        <p><b>등록번호:</b> <%= registrationNumber %> / <b>제품명:</b> <%= productName || '제품명' %></p>
      <% } %>
      <% if (categoryAndForm) { %><p><b>종류 및 형태:</b> <%= categoryAndForm %></p><% } %>
      <% if (ingredients) { %><p><b>성분량:</b> <%= ingredients %></p><% } %>
    </div>
    <div class="foot">
      <div class="barcode">
        <% if (barcodeBase64) { %>
          <img src="data:image/png;base64,<%= barcodeBase64 %>" alt="barcode"  width="100mm"/>
          <div style="font-size:7pt;color:#333; text-align:center;"> <%= barcodeNumber %> </div>
        <% } %>
      </div>
      <div class="meta">
        <% if (manufactureDate) { %><p><b>제조일자:</b> <%= manufactureDate %></p><% } %>
        <p><b>유통기한:</b> <%= expiryDate || '-' %></p>
      </div>
      <div class="mw">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
          <rect x="3" y="5" width="18" height="14" rx="2" stroke="#0F172A" stroke-width="2"/>
          <rect x="6" y="9" width="9" height="6" rx="1" stroke="#0F172A" stroke-width="2"/>
          <circle cx="18" cy="12" r="1.2" fill="#0F172A"/>
          <circle cx="18" cy="15" r="1.2" fill="#0F172A"/>
        </svg>
        <small>30초~2분</small>
      </div>
    </div>
  </div>
</body>
</html>