<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <style>
    @page { size: 50mm 30mm; margin: 0; }
    * { box-sizing: border-box; }
    html, body { margin:0; padding:0; font-family: 'Nanum Gothic', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif; -webkit-print-color-adjust: exact; print-color-adjust: exact; }
    .label { width: 50mm; height: 30mm; padding: 1.5mm; border: 0.2mm solid #ccc; display:flex; flex-direction:column; justify-content:center; align-items:center; text-align:center; }
    .line { font-size: 7pt; margin-bottom: 1mm; }
    .digit { letter-spacing: 0.6mm; }
  </style>
</head>
<body>
  <div class="label">
    <div class="line"><b>제 조 날 짜</b></div>
    <div class="line digit"><%= (manufactureDate || '-').split('').join(' ') %></div>
    <div class="line"><b>유 통 기 한</b></div>
    <div class="line digit"><%= (expiryDate || '-').split('').join(' ') %></div>
  </div>
</body>
</html>