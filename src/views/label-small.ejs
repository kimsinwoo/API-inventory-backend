<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <title>Small 라벨</title>
    <style>
      @page { size: 50mm 30mm; margin: 0; }
      body { margin: 0; padding: 0; font-family: 'Nanum Gothic', sans-serif; }
      .label-small {
        width: 100%; height: 100%; border: 1px solid #aaa;
        display: flex; flex-direction: column; justify-content: center; align-items: center;
        font-size: 7pt; box-sizing: border-box;
      }
      p { margin: 2px 0; text-align: center; }
    </style>
  </head>
  <body>
    <div class="label-small">
      <p><strong>제 조 날 짜</strong></p>
      <p><%= manufactureDate ? manufactureDate.split('').join(' ') : '-' %></p>
      <p><strong>유 통 기 한</strong></p>
      <p><%= expiryDate ? expiryDate.split('').join(' ') : '-' %></p>
    </div>
  </body>
</html>
