<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <style>
    @page { size: 28mm 16mm; margin: 0; }
    * { box-sizing: border-box; }
    html, body { margin:0; padding:0; font-family: 'Nanum Gothic', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif; -webkit-print-color-adjust: exact; print-color-adjust: exact; }
    .label { width: 28mm; height: 16mm; padding: 1mm; border: 0.2mm solid #ccc; display:flex; align-items:center; justify-content:center; gap: 1.5mm; overflow:hidden; }
    .rotate { transform: rotate(-90deg); font-size: 6pt; font-weight: 700; white-space: nowrap; margin-left: -15mm; }
    .barcode img { height: auto; margin-left: -5mm; }
  </style>
</head>
<body>
  <div class="label">
    <div class="rotate"><%= productName || '제품명' %></div>
    <div class="barcode">
      <% if (barcodeBase64) { %>
        <img src="data:image/png;base64,<%= barcodeBase64 %>" alt="barcode" width="40mm"/>
      <% } %>
    </div>
  </div>
</body>
</html>